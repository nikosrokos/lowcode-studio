<?xml version="1.0" encoding="utf-8"?>
<Activity mc:Ignorable="sap sapc" x:Class="Main"
  xmlns="http://schemas.microsoft.com/netfx/2009/xaml/activities"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:sap="http://schemas.microsoft.com/netfx/2009/xaml/activities/presentation"
  xmlns:sap2010="http://schemas.microsoft.com/netfx/2010/xaml/activities/presentation"
  xmlns:ui="http://schemas.uipath.com/workflow/activities"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Sequence DisplayName="Main">
    <Sequence.Variables>
      <Variable x:TypeArguments="x:String" Default="Hello" Name="message" />
      <Variable x:TypeArguments="x:Int32" Default="1" Name="counter" />
    </Sequence.Variables>
    <ui:LogMessage DisplayName="Log Message" Level="Info" Message="[&quot;Starting&quot;]" />
    <Assign DisplayName="Assign counter">
      <Assign.To>
        <OutArgument x:TypeArguments="x:Int32">[counter]</OutArgument>
      </Assign.To>
      <Assign.Value>
        <InArgument x:TypeArguments="x:Int32">[2]</InArgument>
      </Assign.Value>
    </Assign>
    <If Condition="[counter &gt; 0]" DisplayName="If has work">
      <If.Then>
        <Sequence DisplayName="Then">
          <ui:LogMessage DisplayName="Then log" Level="Info" Message="[&quot;yes&quot;]" />
        </Sequence>
      </If.Then>
      <If.Else>
        <Sequence DisplayName="Else">
          <ui:LogMessage DisplayName="Else log" Level="Warn" Message="[&quot;no&quot;]" />
        </Sequence>
      </If.Else>
    </If>
    <ui:InvokeWorkflowFile DisplayName="Process" WorkflowFileName="Framework\\Process.xaml" />
    <Delay DisplayName="Wait" Duration="00:00:01" />
  </Sequence>
</Activity>
